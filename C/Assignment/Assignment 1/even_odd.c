#include <stdio.h>
int main(){
    int number = 10 ;

    if(number % 2 == 0){
        printf("The number %d is a Even number",number);
    } else{
        printf("The number %d is a odd number",number);
    }
}