#include <stdio.h>
int main (){
    int km = 1 ;
    int meter = 200 ;
    int total ;

    int new =km*1000 ;  // km in meter convert

    total = new + meter ;

    printf("Total in meter = %d",total);

   
}